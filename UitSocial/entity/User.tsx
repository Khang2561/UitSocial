export interface User {
  id: string;
  name?: string;
  email?: string;
  image?: string;
  phoneNumber?: String;
  address?: string; // Optional
  bio?: string;
  // Thêm các thuộc tính khác nếu cần
}